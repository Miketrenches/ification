{
  "functions": {
    "api/transform.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
```

5. Click **"Commit changes"**

That's it! Your repo should now look like:
```
your-repo/
├── api/
│   └── transform.js
├── ification.html
├── vercel.json  ← new file here
